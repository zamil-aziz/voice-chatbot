# MLX - Apple Silicon optimized ML framework
# MLX 0.31.x currently SIGBUS-crashes on this Mac when imported with Kokoro/PyTorch.
mlx==0.30.1
mlx-lm==0.30.0

# Whisper for Speech-to-Text
mlx-whisper>=0.4.0

# Audio processing
sounddevice>=0.5.0
numpy>=1.26.0,<2.4.0
scipy>=1.14.0,<2.0.0

# Voice Activity Detection
silero-vad>=5.1

# TTS - Kokoro (realistic voices)
kokoro>=0.9.4

# Utilities
rich>=13.9.0  # Pretty console output
pydantic>=2.9.0  # Config validation
typer==0.21.0  # Kokoro/spaCy import dependency; 0.24.x installed metadata-only in this venv
